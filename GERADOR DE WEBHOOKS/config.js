module.exports = {
    // Token do bot (pegue no Discord Developer Portal → Bot → Token)
    token: '',

    // Client ID do bot (pegue no Discord Developer Portal → General Information → Application ID)
    clientId: '',

    // ID do servidor onde o bot vai operar (clique com botão direito no servidor → Copiar ID do servidor)
    guildId: '',

    // URL da imagem padrão usada como avatar das webhooks
    webhookAvatarURL: '',
};
